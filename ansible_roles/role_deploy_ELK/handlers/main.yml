---
# handlers file for deploy_ELK
- name: restart_ELK
  service:
    name: "{{ item }}"
    state: restarted
    enabled: yes
  loop: "{{ ELK }}"
- name: persist_elk
  service:
     name: "{{ item }}"
     state: started
     enabled: yes
     daemon-reload: yes
  loop: "{{ ELK }}"

